<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Chess!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body 
    data-js-view-name="<%= js_view_name(@conn, @view_template) %>" 
    data-view-name="<%= view_name(@conn) %>"
    data-template-name="<%= template_name(@view_template) %>">

    <%= render ChessWeb.PageView, "navbar.html", assigns %>
    <div class="container-fluid">
        <%= if get_flash(@conn, :info) do %>
          <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
        <% end %>
        <div id="alert-block" class="d-none">
          <p class="alert alert-info" role="alert"></p>
        </div>

        <%= if get_flash(@conn, :error) do %>
          <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
        <% end %>
        <div id="danger-block" class="d-none">
          <p class="alert alert-danger" role="alert"></p>
        </div>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>
    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
